import { PracticeStatePipe } from './practice-state.pipe';

describe('PracticeStatePipe', () => {
  it('create an instance', () => {
    const pipe = new PracticeStatePipe();
    expect(pipe).toBeTruthy();
  });
});
