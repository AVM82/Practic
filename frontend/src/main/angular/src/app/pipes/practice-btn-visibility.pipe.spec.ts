import { PracticeBtnVisibilityPipe } from './practice-btn-visibility.pipe';

describe('PracticeBtnVisibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new PracticeBtnVisibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
